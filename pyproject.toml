[project]
name = "drawflow-mcp"
version = "0.1.0"
description = "MCP server that renders agent-authored graph specs into graph and detail PNG downloads."
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.4",
    "grandalf>=0.8",
    "pillow>=12.2.0",
    "pydantic>=2.13.3",
]

[tool.uv]
package = false
